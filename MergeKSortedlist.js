function processNestedArrays(lists) {
    let x = lists.flat();
  }
  
  let inputLists = [[1, 4, 5], [1, 3, 4], [2, 6]];
  let outputResult = processNestedArrays(inputLists);
  console.log(outputResult);